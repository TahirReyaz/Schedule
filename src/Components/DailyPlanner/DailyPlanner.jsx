import React from 'react';
import Grid from '@material-ui/core/Grid';

const DailyPlanner = () => {

    return (
        <Grid>
            <h1>These are the days we have been waiting for</h1>
        </Grid>
    );
}

export default DailyPlanner;